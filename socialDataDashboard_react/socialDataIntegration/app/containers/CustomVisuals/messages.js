/*
 * CustomVisuals Messages
 *
 * This contains all the text for the CustomVisuals container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomVisuals';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomVisuals container!',
  },
});
