/*
 * EventDashboard Messages
 *
 * This contains all the text for the EventDashboard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EventDashboard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Dashboard for Event',
  },
});
