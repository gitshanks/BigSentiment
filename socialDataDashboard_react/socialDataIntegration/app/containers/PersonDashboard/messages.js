/*
 * PersonDashboard Messages
 *
 * This contains all the text for the PersonDashboard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PersonDashboard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonDashboard container!',
  },
});
