/*
 * PlaceDashboard Messages
 *
 * This contains all the text for the PlaceDashboard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PlaceDashboard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PlaceDashboard container!',
  },
});
