/*
 * ParticlesBackground Messages
 *
 * This contains all the text for the ParticlesBackground component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ParticlesBackground';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ParticlesBackground component!',
  },
});
