/*
 * WorldMap Messages
 *
 * This contains all the text for the WorldMap component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WorldMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WorldMap component!',
  },
});
