/*
 * TopicInfo Messages
 *
 * This contains all the text for the TopicInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TopicInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopicInfo component!',
  },
});
