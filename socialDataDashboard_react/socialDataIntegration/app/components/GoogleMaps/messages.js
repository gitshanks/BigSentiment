/*
 * GoogleMaps Messages
 *
 * This contains all the text for the GoogleMaps component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GoogleMaps';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleMaps component!',
  },
});
